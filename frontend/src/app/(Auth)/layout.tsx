
const RegisterPage = ({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) => {

    const register = (
        <>
          {children}
        </>
    )

    return register;
}

export default RegisterPage;